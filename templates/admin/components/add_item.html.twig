<div class="d-flex justify-content-center mb-4">
    <a
            href="{{ addItemRoute }}"
            data-toggle="tooltip"
            data-placement="left"
            title="{{ tooltipMessage }}">
        <img src="{{ asset('default/image/plus.png') }}"
             alt="add item"
             width="40"
             height="40"
             class="d-inline-block align-text-top">
    </a>
</div>